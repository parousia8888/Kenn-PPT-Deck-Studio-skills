Act / slide-kind marker — sits above the title on every slide as a tracked mono label with a glowing accent tick.

```jsx
<Kicker index={4} total={10}>Product Reveal</Kicker>
```

Omit `index` for a plain label. Keep the text short and upper-case in spirit (it renders uppercase).
