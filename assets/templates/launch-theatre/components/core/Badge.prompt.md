Small tracked-mono status chip.

```jsx
<Badge variant="accent">Preview</Badge>
<Badge dot>Live</Badge>
<Badge variant="solid">New</Badge>
```

Variants: `default`, `accent`, `solid`, `muted`. `dot` adds a glowing live indicator.
