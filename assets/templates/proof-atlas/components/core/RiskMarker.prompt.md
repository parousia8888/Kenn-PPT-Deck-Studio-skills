A restrained risk / limitation row — oxide marker and 2px oxide left rule, factual tone. The only place warm color appears.

```jsx
<RiskMarker level="limitation" id="L-04" title="Sample skews to early adopters.">
  Results may overstate effect for the general population; weight before extrapolating.
</RiskMarker>
```

Levels: `limitation`, `risk`, `caveat`. Keep copy honest and brief — never alarmist.
