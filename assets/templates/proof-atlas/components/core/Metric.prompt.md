The signature large, light-weight number — Manrope 200 — with unit, label, delta, and a built-in source line.

```jsx
<Metric value="84.2" unit="%" label="Findings corroborated by 2+ sources"
        delta="+9 pts" method="audit panel" id="n=412" size="xl" />
```

Sizes `xl` / `l` / `m` / `s`. Delta auto-colors: green up, oxide down. Always pass `method`/`id` so the number is sourced.
