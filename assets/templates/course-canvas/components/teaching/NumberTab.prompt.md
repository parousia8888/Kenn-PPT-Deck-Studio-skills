Big serif section-number that anchors a slide or panel.

```jsx
<NumberTab value="04" label="of 12 · Unit II" />
<NumberTab value="M2" variant="sage" size={84} />
```

`value` is the numeral/code; `label` is an optional mono caption; `variant` is `ox` or `sage`; `size` sets the numeral px size.
