Small annotation for the margin gutter — a thin left rule with an optional mono label.

```jsx
<MarginNote label="Etymology" variant="ox">
  From Latin <i>norma</i> — a carpenter's square.
</MarginNote>
```

Variants: `default` (graphite rule), `ox`, `sage`. Width is capped to the gutter measure.
