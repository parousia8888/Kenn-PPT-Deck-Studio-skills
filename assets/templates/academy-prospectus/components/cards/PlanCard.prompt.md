An enrollment / pricing column for the contact page — tier label, serif price, accent-bulleted inclusions.

```jsx
<PlanCard
  tier="Scholar · Most chosen"
  price="¥ 11,400"
  feature
  items={["Four seminars each week", "One-to-one mentor hour", "Examination studio access"]}
/>
```

Set `feature` on one column to mark the recommended tier. Inherits the section `--accent`.
