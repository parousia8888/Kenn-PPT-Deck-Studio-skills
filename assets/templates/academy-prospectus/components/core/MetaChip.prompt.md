The signature mono metadata token for course codes, age groups, durations and module labels.

```jsx
<MetaChip>LA · 309</MetaChip>
<MetaChip bare>Ages 14–17</MetaChip>
```

Filled by default (tinted with the section's `--accent-field`); pass `bare` for plain tracked mono text inside cards.
